name: mantra
channels:
  - defaults
dependencies:
  - pip
  - pydantic
  - pyyaml
  - validators
  - pytest
  - ruff
  - huggingface_hub
  - numpy
  - pytorch
  - pip:
      - build>=1.2,<2
      - httpx>=0.28,<1
      - pyright>=1.1.400,<2
      - torchdata==0.11.0
      - twine>=6,<7
prefix: /Users/machina/miniconda3/envs/mantra
