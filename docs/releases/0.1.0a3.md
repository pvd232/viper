# VIPER 0.1.0a3

Published on September 11, 2026: [PyPI](https://pypi.org/project/viper-provenance/0.1.0a3/).

```bash
python -m pip install "viper-provenance==0.1.0a3"
```

## Release identity

- Source commit: `baeaf1261f6ef9e07cbabbae3262a88d5ec6dbf5`.
- Signed tag: [`v0.1.0a3`](https://github.com/pvd232/viper/releases/tag/v0.1.0a3).
- [CI run](https://github.com/pvd232/viper/actions/runs/34611415601) built the distributions.
- [Publication run](https://github.com/pvd232/viper/actions/runs/34613101043) published those files through TestPyPI and PyPI.

The [release receipt](receipts/0.1.0a3.json) records distribution sizes, hashes,
and validation results. Both registries serve the same bytes used for GPU acceptance.
The signed tag identifies the CI run and the acceptance archive's SHA-256.

## Validation

- Python 3.11–3.14: 390 unit and contract tests passed on each interpreter.
- Python 3.14: 125 integration tests and three release tests passed.
- Source distribution installed on the L4 host: 14 public-API tests passed.
- Wheel installed on that host: 18 public-API, CPU quickstart, and generated-workflow tests passed.
- NVIDIA L4: all nine live CUDA tests passed, including runtime controls and repeated reproducible output bytes.
- Public PyPI installation on macOS: 17 public-API and CPU quickstart cases passed.
- MANTRA's CUDA ridge solver agreed with its NumPy reference within the declared tolerance.

The [GPU acceptance archive](https://github.com/pvd232/viper/releases/download/v0.1.0a3/viper-0.1.0a3-gpu-evidence.tar.gz)
contains the test results, runtime receipts, scripts, and generated run records.
The temporary VM, boot disk, NAT gateway, and router were removed after the
archive was copied and its hash verified.

The [earlier GPU receipt](receipts/v0.1.0a3-mantra-gpu.json) describes an earlier
candidate; the release receipt above identifies the published files.

## Incompatible API changes

- Python run and benchmark results expose `status`, `path`, `record`, and
  `reference`. Replace `resolved_run_path` and `result_path` on these wrappers
  with `path`; replace `resolved_run` and `result` with `record`, and
  `resolved_run_ref` and `result_ref` with `reference`. The nested result in a
  batch uses the same names. Stored terminal records keep their existing fields.
- Import objects from their defining modules. Package-root
  re-exports of individual classes and functions have been removed.
- Use `viper.config` classes and the `config=` argument for stage and metric
  configuration. Replace parameter-model declarations with config classes.
- Declare stage outputs with `StageOutputs`, `TrainOutputs`, or `EvalOutputs`
  and `output()`. Stage functions write through `context.outputs`; completed
  files are recorded as artifacts.
- Training uses the names `model` and `resume_state`. Evaluation reads `model`
  and `test` and writes `predictions`.
- Stage documents use schema version 2. Regenerate earlier stage documents
  through the current authoring API.
- Use `serialize_document()`; the `serialize_record()` alias is removed.

The [Python API](../reference/api.md) and [configuration guide](../reference/configuration.md)
show the current imports and declarations.

## Earlier releases

- [0.1.0a2](0.1.0a2.md)
- [0.1.0a1](0.1.0a1.md)
