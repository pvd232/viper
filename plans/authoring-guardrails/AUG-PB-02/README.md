# AUG-PB-02

This plan adds a deterministic matrix constructor that obtains exactly one
named variant for every declared factor-level cell. It rejects omitted cells,
changed cell assignments, and duplicate variant IDs before experiment
construction.
