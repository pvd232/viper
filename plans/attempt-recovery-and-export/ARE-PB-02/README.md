# ARE-PB-02

This plan exports a verified successful or failed terminal run as a portable
directory bundle. The bundle retains every traversed evidence payload, complete
snapshot membership, deterministic identities, and an offline-only verifier.
